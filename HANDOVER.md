# สถานะงาน ณ 2026-08-07 10:40 — สำหรับส่งต่อแชทใหม่

อ่านไฟล์นี้คู่กับ `CLAUDE.md` (กฎเหล็ก + โครงสร้าง) ก่อนเริ่มงานต่อ

---

## 1. งานรายวัน — เสร็จแล้ว ใช้งานได้จริง

**13 ร้าน · ระบบดึงเองได้ครบทุกร้าน** (พิสูจน์กับของจริง 2026-08-07)

```
08:30  DealerMKP-DailyOrders  ดึง 13 ร้าน (ข้อมูลของเมื่อวาน)
~08:50 ส่งอีเมลท้ายรอบ
09:00  DealerMKP-DailyMail    ตาข่ายรองรับ (ข้ามถ้าส่งแล้ว)
```

ผู้รับ: `Pitchaya.L@` + สำเนา `Natcha.S@` `Tanapoom.S@` `panupun.s@` `Narissa.W@`

**กฎที่เจ้าของงานสั่ง: ไม่ครบ 13 ร้าน ห้ามส่งเมล** (`--only-if-complete` ใน `run_daily.ps1`)
⚠️ ผลข้างเคียง: วันไหนพังจะไม่มีอีเมลเลย ต้องดู Dashboard เอง

---

## 2. งานดึงย้อนหลัง 7 เดือน (1 ม.ค. – 31 ก.ค. 2026) — ✅ จบแล้ว 2026-08-07

**ครบ 13 ร้าน · 660,181 ออเดอร์** · ตรวจความถูกต้องผ่านครบ 5 ข้อ

```
Shopee   453,959  (69%)
TikTok   192,129  (29%)
Lazada    14,093  ( 2%)
```

รายงาน 10 ไฟล์อยู่ที่ `output/_report_2026h1/` และอัปขึ้น OneDrive/SharePoint แล้ว
โฟลเดอร์ `รายงานยอดขายย้อนหลัง 13 ร้าน 2026-01 ถึง 2026-07`

- **ไฟล์รวม 1 ไฟล์** ทุกร้านทุกแพลตฟอร์มในชีทเดียว (660,181 แถว ยังไม่ถึง 1 ล้าน ไม่ต้องแตกชีท)
- **ไฟล์รายแบรนด์ 9 ไฟล์** ไฟล์ละ 4 ชีท: รวม / Lazada / TikTok / Shopee
  แพลตฟอร์มที่แบรนด์นั้นไม่ได้ขาย เขียนว่า "ไม่มีข้อมูล" ไม่ปล่อยชีทว่าง

**9 แบรนด์ ไม่ใช่ 13** เพราะร้านเดียวกันขายหลายแพลตฟอร์มต้องยุบเป็นไฟล์เดียว —
การจับกลุ่มอยู่ใน `config/brands.yaml` ⚠️ **ห้ามเดาเอง** ต้องให้เจ้าของงานยืนยัน
(ยืนยันแล้ว: powerstool=Powerstools · Toolspartner=TNLTOOLSTORE)

```powershell
.\.venv\Scripts\python.exe -u scripts\build_reports.py                  # สร้างใหม่ทั้งหมด
.\.venv\Scripts\python.exe -u scripts\build_reports.py --only-brands    # แก้ brands.yaml แล้วทำเฉพาะไฟล์แบรนด์
.\.venv\Scripts\python.exe -u scripts\verify_reports.py                 # ตรวจก่อนส่ง (ใช้เวลา ~20 นาที)
```

⚠️ **ต้องใส่ `-u` เสมอ** ไม่งั้น Python บัฟเฟอร์ stdout ตอนถูก redirect แล้วมองไม่เห็นความคืบหน้าเลย

---

## 3. บทเรียนสำคัญ — อย่าทำผิดซ้ำ

### 3.1 ห้ามฆ่า Chrome ด้วย Stop-Process
cookie ที่ยังอยู่ในหน่วยความจำจะไม่ถูกเขียนลงดิสก์ **ล็อกอินที่เพิ่งทำหายทั้งดุ้น**
เคยทำพลาดแล้วทำให้ผู้ใช้ต้องล็อกอินซ้ำหลายรอบ

### 3.2 Chrome ค้างทำให้เรนเดอร์หน้าไม่ครบ
ถ้ามี process ของ `data/profiles/*` ค้างอยู่ จะเปิด Chrome ตัวจริงไม่ได้
ต้องถอยไปใช้ Chromium เปล่า → หน้า Shopee เรนเดอร์ไม่ครบ → หน้าเลือกร้านว่าง (tr=0)
**เช็คก่อนเริ่มงานเสมอ:**
```powershell
Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" | Where-Object { $_.CommandLine -like "*data\profiles*" }
```

### 3.3 Shopee ตั้งภาษาได้รายร้าน
`shopee_03`/`shopee_08` เป็นภาษาอังกฤษ ร้านอื่นเป็นไทย
selector ที่ผูกกับข้อความต้องมีทั้ง 2 ภาษาเสมอ — ตรวจแล้วครบทุกจุด ณ commit `68e806e`

### 3.4 ตัวรอ (waiter) ห้ามนับตัวเอง
เคยเขียน PowerShell รอ process ที่มีคำว่า `backfill` แต่ตัวมันเองก็มีคำนั้น → รอตลอดกาล
**เลี่ยงการใช้ waiter — รันต่อกันในคำสั่งเดียวไปเลย**

### 3.5 PowerShell เขียนไฟล์ทับ = พัง
`Set-Content -Encoding utf8` ใส่ BOM (JSON พัง) และทำภาษาไทยเพี้ยน
**ใช้ Edit tool เท่านั้นกับไฟล์ที่มีภาษาไทย**

---

## 4. ปัญหาที่แก้แล้ว (อย่ารื้อ)

| ปัญหา | วิธีแก้ |
|---|---|
| session หมดแล้วยอมแพ้ | `auto_relogin` ครบ 3 แพลตฟอร์ม |
| Shopee relogin ต้องใช้รหัสผ่าน | ใช้ account chooser แทน — **ไม่ต้องใช้รหัส** |
| ป๊อปอัปโฆษณาทับปุ่ม | `_clear_overlay_over()` ใช้ elementFromPoint |
| สลับร้านที่ 2 ของบัญชีไม่ได้ | `/portal/shop` ห้ามใส่ `?next=` ตอนบังคับกลับ |
| session ดีถูกเขียนทับ | `_save_session_if_logged_in()` |
| Lazada ไม่กรองวันที่ | Export All ส่งทุกออเดอร์ → กรองเองในเครื่อง |

### auto_relogin ของ Shopee — จุดสำคัญ
ต้องเข้า `accounts.shopee.co.th/seller/login?next=...` **โดยตรง**
ถ้าใช้ `base_url + login_path` จะถูกเด้งไป `shopee.co.th/seller/login` ซึ่งไม่มีปุ่มบัญชีหลัก/บัญชีย่อย

---

## 5. งานที่ค้างอยู่

1. **ตัวเคลียร์ Chrome ค้างอัตโนมัติก่อนเริ่มรอบ** — เสนอไว้ ยังไม่ได้ทำ รอเจ้าของงานตัดสินใจ
   เป็นความเสี่ยงเดียวที่เหลือของงานรายวันซึ่งแก้ได้ด้วยโค้ด

งานย้อนหลัง 7 เดือนจบแล้ว (ดูหัวข้อ 2) · shopee_07/09/10 เอาออกถาวรแล้ว

---

## 6. สคริปต์ที่มีให้ใช้

```powershell
.\run_daily.ps1 -Shop shopee_03           # ดึงร้านเดียว
.\watch_run.ps1                            # ดูรอบรายวันสด
.\watch_backfill.ps1                       # ดูงานย้อนหลังสด
.\.venv\Scripts\python.exe scripts\login_save.py --shop shopee_03      # ล็อกอิน+เซฟอัตโนมัติ
.\.venv\Scripts\python.exe scripts\test_relogin.py --shop shopee_03    # ทดสอบ relogin
.\.venv\Scripts\python.exe scripts\import_manual.py --shop shopee_03 --latest  # นำเข้าไฟล์ที่โหลดเอง
.\.venv\Scripts\python.exe scripts\open_shop_browser.py --shop shopee_03  # เปิดเบราว์เซอร์ให้คนใช้
```

**เครื่องมือวินิจฉัย** (`scripts/diag_*.py`) — ใช้ดู DOM จริงก่อนเขียน selector
บทเรียน: **ดูของจริงก่อนเดา** เสียเวลาน้อยกว่าเดาแล้วแก้หลายรอบมาก

---

## 7. สิ่งที่เจ้าของงานสั่งไว้ชัดเจน

- ตอบเป็นภาษาไทย กระชับ เน้นตัวเลขและสิ่งที่ต้อง action
- **ไม่ต้องการทำเอง** — ระบบต้องดึงเองได้ ไม่ใช่ให้คนมา export มือทุกวัน
- ให้ตรวจสอบและ**ชิงแก้ปัญหาก่อนเกิด** ไม่ใช่รอให้พังแล้วค่อยแก้
- งานย้อนหลังกับงานรายวัน **แยกกันเด็ดขาด** ห้ามให้กระทบกัน
- ถ้ามีวิดีโอ/ภาพส่งมา **ให้ดูทันที** (ลง `opencv-python-headless` แล้ว ใช้แตกเฟรมได้)
